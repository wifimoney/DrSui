import type { FileReader } from '../file.js';
import type { QuiltReader } from './quilt.js';
export interface QuiltBlobHeader {
    identifier: string;
    tags: Record<string, string> | null;
    blobSize: number;
    contentOffset: number;
    columnSize: number;
}
export declare class QuiltFileReader implements FileReader {
    #private;
    constructor({ quilt, sliverIndex, identifier, tags, }: {
        quilt: QuiltReader;
        sliverIndex: number;
        identifier?: string;
        tags?: Record<string, string>;
    });
    getBytes(): Promise<Uint8Array>;
    getIdentifier(): Promise<string>;
    getTags(): Promise<Record<string, string>>;
}
