import type { FileReader } from '../file.js';
export declare class LocalReader implements FileReader {
    #private;
    constructor({ contents, identifier, tags, }: {
        contents: Uint8Array | Blob;
        identifier?: string;
        tags?: Record<string, string>;
    });
    getBytes(): Promise<Uint8Array<ArrayBufferLike>>;
    getIdentifier(): Promise<string | null>;
    getTags(): Promise<Record<string, string>>;
}
