import type { WalrusClient } from '../client.js';
import type { BlobReader } from './readers/blob.js';
import { WalrusFile } from './file.js';
export declare class WalrusBlob {
    #private;
    constructor({ reader, client }: {
        reader: BlobReader;
        client: WalrusClient;
    });
    asFile(): WalrusFile;
    blobId(): Promise<string | null>;
    files(filters?: {
        ids?: string[];
        tags?: {
            [tagName: string]: string;
        }[];
        identifiers?: string[];
    }): Promise<WalrusFile[]>;
    exists(): Promise<boolean>;
    storedUntil(): Promise<number | null>;
}
